# BeeBeeBrain - AI 工程管理平台

> **核心理念:** 从 "AI 结对编程 (Copilot)" 转型为 **"AI 团队管理 (Manager)"**
>
> **产品定位:** 面向超小型团队/PM开发者的 **AI 工程管理平台**
>
> **核心隐喻:** 用户是指挥官, Agent 是可并行的、有角色的虚拟劳动力

---

## 📋 目录

- [产品定义](#产品定义)
- [核心体验流程](#核心体验流程)
- [系统架构](#系统架构)
- [技术栈选型](#技术栈选型)
- [开发指南](#开发指南)
- [MVP 策略](#mvp-策略)
- [项目结构](#项目结构)

---

## 🎯 产品定义

### 解决的问题

当前 AI 编程工具（Claude Code/Cursor）的痛点:
- ❌ **串行效率低** - 等待一个 Agent 一个字蹦出来
- ❌ **上下文污染** - 单一聊天窗口承载过多信息
- ❌ **人类负担重** - 需要像保姆一样盯着 AI 生成

### 产品形态

**Linear (任务管理)** + **Feishu (协作文档)** + **Virtual Workforce (虚拟员工)**

### 核心价值

1. **并行感 (Parallelism)** - 从"等一个字一个字蹦"变成"看五个进度条同时跑"
2. **管理感 (Agency)** - 从"写 Prompt"变成"派 Ticket"
3. **确定性 (Determinism)** - 通过"黄金模板"和"核心宪法"保证生成的项目结构统一、可运行

---

## 🔄 核心体验流程

### 1. 启动阶段 (The Blueprint)

**交互方式:** 聊天式需求访谈 → 实时生成架构图/思维导图

**MVP 策略 - 强制使用黄金模板:**
```
Next.js + Prisma + Tailwind + TypeScript
```

- ✅ 跳过繁琐的基础配置
- ✅ 确保 Agent 即使在无上下文情况下也能遵循最佳实践
- ✅ 保证生成的项目结构统一

### 2. 执行阶段 (The Hustle)

#### 任务分裂系统

系统将需求自动拆解为**并行**的 Ticket（任务卡片）

#### 可视化看板 (Dashboard)

抛弃单一聊天窗口,采用 Dashboard 视图:

```
┌─────────────────────────────────────────────────────┐
│  🤖 Agent 状态                                        │
├─────────────────────────────────────────────────────┤
│  🔵 前端 Agent A  [████████░░] 80%                   │
│  🟢 后端 Agent B  [██████████] 100%                  │
│  🔵 设计 Agent C  [████░░░░░░] 40%                   │
├─────────────────────────────────────────────────────┤
│  📋 任务看板                                          │
├─────────────────────────────────────────────────────┤
│  Gray (待办)  │  Blue (进行中)  │  Green (完成)      │
│  ────────────┼─────────────────┼───────────────    │
│  Task D      │  Task A         │  Task B            │
│  Task E      │  Task C         │                     │
└─────────────────────────────────────────────────────┘
```

#### 依赖管理

- 可视化的依赖链（Task B 等待 Task A 完成）
- 底层自动处理上下文注入
- 自动调度: 当依赖满足时,任务自动开始

### 3. 验收与干预 (Review & Nudge)

#### 实时预览
随着文件生成,右侧预览窗口自动刷新,从白屏到完整 App

#### 上帝之手
用户不仅是 Reviewer,更是 Editor:
- ✅ 随时"拍肩膀"打断 Agent
- ✅ 广播指令（如"修改主题色"）给所有正在工作的 Agent
- ✅ 手动调整任务优先级

---

## 🏗️ 系统架构

### 1. 并行引擎 (The Engine)

#### 核心技术: Git Worktree

```bash
# 不为每个 Agent 克隆仓库,而是创建轻量级工作区
git worktree add ../agent-a-frontend frontend-branch
git worktree add ../agent-b-backend backend-branch
git worktree add ../agent-c-design design-branch
```

**优势:**
- ✅ 真正的物理隔离,文件修改互不干扰
- ✅ 节省磁盘空间（共享 .git 对象）
- ✅ 独立的分支管理

#### 分支策略

```
mainline (主线)
  ├─ shadow/frontend-a
  ├─ shadow/backend-b
  ├─ shadow/design-c
  └─ shadow/testing-d
```

### 2. 冲突解决漏斗 (Conflict Resolution)

```
Layer 1: Git 自动合并 (无感)
    ↓ 失败
Layer 2: Agent 自修复 (AI 分析冲突并重写)
    ↓ 失败
Layer 3: 人类介入 (Merge UI + AI 建议)
```

### 3. 知识系统 (The Brain)

#### L1 核心宪法 (Constitution)

动态注入 System Prompt 的规则:
- 📝 技术栈指纹 (Tech Stack Fingerprint)
- 📂 目录结构规范 (Directory Structure)
- 🚫 不可违背的规范 (Non-negotiable Rules)

#### L2 技能库 (Skills Library)

基于 AST 索引的按需工具:
```typescript
// 示例: 防止幻觉和重复造轮子
const skills = {
  findComponent: (name: string) => Component | null,
  checkSchema: (table: string) => Schema | null,
  locateEndpoint: (path: string) => Route | null
}
```

---

## 🛠️ 技术栈选型

### 前端技术栈 (黄金模板)

```json
{
  "framework": "Vue 3 + TypeScript",
  "build": "Vite",
  "ui": "TailwindCSS + shadcn-vue",
  "state": "Pinia",
  "router": "Vue Router",
  "http": "axios",
  "monaco": "@monaco-editor/react" // 代码预览
}
```

**选型理由:**
- Vue 3: 响应式系统适合实时更新的 Dashboard
- TailwindCSS: 快速构建现代 UI
- shadcn-vue: 高质量组件库,避免重复造轮子
- Monaco Editor: VS Code 同款编辑器,代码预览体验

### 后端技术栈

```json
{
  "runtime": "Node.js + TypeScript",
  "framework": "Express / Fastify",
  "orm": "Prisma",
  "database": "SQLite (开发) / PostgreSQL (生产)",
  "queue": "BullMQ", // 任务队列
  "websocket": "Socket.IO" // 实时通信
}
```

### 并行执行引擎

```json
{
  "versionControl": "Git Worktree",
  "process": "Worker Threads / Child Process",
  "ipc": "WebSocket / IPC Channel",
  "isolation": "FileSystem Sandbox"
}
```

---

## 📖 开发指南

### 环境准备

```bash
# 1. 克隆项目
git clone <repo-url>
cd beebeebrain

# 2. 安装依赖
pnpm install

# 3. 启动开发服务器
pnpm dev
```

### 核心模块说明

#### 1. Agent 调度器 (`/src/engine/scheduler.ts`)

负责任务分发和 Agent 生命周期管理:
```typescript
class AgentScheduler {
  // 分配任务给 Agent
  assign(ticket: Ticket, agent: Agent): Promise<void>

  // 监听任务完成
  onTicketComplete(handler: (ticket: Ticket) => void): void

  // 处理依赖关系
  resolveDependencies(ticket: Ticket): Promise<boolean>
}
```

#### 2. Git Worktree 管理器 (`/src/engine/worktree.ts`)

负责创建和管理 Agent 工作区:
```typescript
class WorktreeManager {
  // 创建新的工作区
  create(branchName: string): Promise<string>

  // 清理工作区
  remove(worktreePath: string): Promise<void>

  // 合并分支
  merge(sourceBranch: string, targetBranch: string): Promise<void>
}
```

#### 3. 知识系统 (`/src/brain/constitution.ts`)

负责生成和注入上下文:
```typescript
class Constitution {
  // 生成系统提示词
  generateSystemPrompt(techStack: string): string

  // 索引代码库
  indexRepository(): Promise<CodeIndex>

  // 查询相关代码
  queryCode(query: string): Promise<CodeSnippet[]>
}
```

### 开发规范

#### 代码风格
- 使用 ESLint + Prettier
- 遵循 TypeScript 严格模式
- 组件命名: PascalCase
- 工具函数: camelCase
- 常量: UPPER_SNAKE_CASE

#### Git 提交规范
```
<type>(<scope>): <subject>

type:
- feat: 新功能
- fix: 修复
- docs: 文档
- style: 格式
- refactor: 重构
- test: 测试
- chore: 构建
```

---

## 🚀 MVP 策略

### 范围限制

**✅ 仅限新项目 (Greenfield)**
- 暂时放弃导入旧代码（Legacy Code）的复杂场景
- 专注于"从 0 到 1"的爽感

### 演示优先 (Demo-First Development)

**目标:** 用于融资或展示概念,不追求后端逻辑完备

#### 实现方案

**前端: 高保真 Vue 应用**
- 看板视图（任务状态）
- 日志流（Agent 输出）
- 文件树动画（代码生成可视化）
- 实时预览窗口

**后端: Mock 剧本引擎**
```json
{
  "timeline": [
    {
      "time": "0s",
      "agents": ["frontend-a", "backend-b"],
      "action": "start",
      "tickets": ["TASK-1", "TASK-2"]
    },
    {
      "time": "5s",
      "agents": ["frontend-a"],
      "action": "complete",
      "files": ["src/App.vue", "src/components/Header.vue"]
    },
    {
      "time": "10s",
      "agents": ["backend-b"],
      "action": "complete",
      "files": ["api/users.ts", "db/schema.prisma"]
    }
  ]
}
```

**产物准备:**
- 📦 "空项目" - 初始状态
- 📦 "完成态" - 完整项目代码
- 🎬 脚本按顺序"蹦出"文件,模拟真实开发过程

---

## 📁 项目结构

```
beebeebrain/
├── claude.md                 # 本文档
├── package.json
├── pnpm-lock.yaml
├── tsconfig.json
├── .gitignore
│
├── docs/                     # 文档目录
│   ├── architecture.md       # 架构设计文档
│   ├── api.md                # API 文档
│   └── contribution.md       # 贡献指南
│
├── frontend/                 # 前端应用 (Vue 3)
│   ├── src/
│   │   ├── components/       # 组件
│   │   │   ├── Dashboard/    # 看板视图
│   │   │   ├── Ticket/       # 任务卡片
│   │   │   ├── AgentStatus/  # Agent 状态
│   │   │   └── LogStream/    # 日志流
│   │   ├── views/            # 页面
│   │   ├── stores/           # Pinia 状态管理
│   │   ├── api/              # API 调用
│   │   └── types/            # TypeScript 类型
│   ├── public/
│   └── vite.config.ts
│
├── backend/                  # 后端应用 (Node.js)
│   ├── src/
│   │   ├── engine/           # 并行执行引擎
│   │   │   ├── scheduler.ts  # Agent 调度器
│   │   │   ├── worktree.ts   # Git Worktree 管理
│   │   │   └── executor.ts   # Agent 执行器
│   │   ├── brain/            # 知识系统
│   │   │   ├── constitution.ts  # 核心宪法
│   │   │   ├── skills.ts        # 技能库
│   │   │   └── indexer.ts       # 代码索引
│   │   ├── api/              # REST API
│   │   ├── models/           # 数据模型
│   │   └── utils/            # 工具函数
│   └── prisma/
│       └── schema.prisma     # 数据库模型
│
├── mock-engine/              # MVP: Mock 剧本引擎
│   ├── scripts/
│   │   └── demo-timeline.json
│   └── server.ts
│
└── shared/                   # 共享代码
    ├── types/                # 共享类型定义
    └── constants/            # 共享常量
```

---

## 🎯 下一步行动

### Phase 1: 基础搭建 (Week 1-2)
- [ ] 初始化前后端项目
- [ ] 搭建基础 UI 框架（Dashboard 布局）
- [ ] 实现 Git Worktree 管理器
- [ ] 编写 Mock 剧本引擎

### Phase 2: 核心功能 (Week 3-4)
- [ ] 实现任务看板视图
- [ ] 实现日志流组件
- [ ] 实现文件树动画
- [ ] 集成 Monaco Editor

### Phase 3: 真实引擎 (Week 5-6)
- [ ] 替换 Mock 引擎为真实 Agent
- [ ] 实现依赖解析
- [ ] 实现冲突解决
- [ ] 接入 LLM API

### Phase 4: 优化与打磨 (Week 7-8)
- [ ] 性能优化
- [ ] UI/UX 优化
- [ ] 文档完善
- [ ] 演示视频制作

---

## 📚 相关资源

- [Claude Code Best Practices](https://docs.anthropic.com/claude-code)
- [Vue 3 官方文档](https://vuejs.org/)
- [Vite 文档](https://vitejs.dev/)
- [TailwindCSS 文档](https://tailwindcss.com/)
- [shadcn-vue 组件库](https://www.shadcn-vue.com/)
- [Git Worktree 文档](https://git-scm.com/docs/git-worktree)
- [Prisma ORM](https://www.prisma.io/)

---

## 🤝 贡献指南

欢迎贡献! 请查看 [docs/contribution.md](docs/contribution.md) 了解详情。

---

## 📄 许可证

MIT License

---

**一句话总结:**

你不是在做一个更好的代码编辑器,你是在做一个 **"软件外包公司的自动化管理后台"**, 用户是老板, AI 是不知疲倦的员工。
